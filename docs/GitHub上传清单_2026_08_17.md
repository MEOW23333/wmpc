# WMPC GitHub 上传清单（2026 年 8 月 17 日）

## 当前状态

- 项目目录：/home/ZhangLexin/PALS/wmpc
- 独立分支：main
- 最近提交：以 git log -1 为准
- 工作树：干净
- 版本库对象大小：约 21 MiB
- 工作目录大小：约 73 MB
- 未包含：轨迹、缓存、整批训练数据、原始日志、构建目录和旧 PALS 历史提交

## 上传前检查

在有网络和 GitHub 权限的机器上执行：

    cd /home/ZhangLexin/PALS/wmpc
    git status --short
    git log --oneline -4
    git remote add origin 新的 GitHub 仓库地址
    git push -u origin main

如果远端仓库已有初始文件，先确认远端内容可丢弃，再执行合并；不要强制覆盖未知远端历史。

## 上传后第一步

1. 克隆新仓库到 GPU 服务器。
2. 重新计算 results/checkpoints/learned_schwarz_v1.pt 和 results/sidecars/ 的哈希。
3. 重新运行 PYTHONPATH=. /home/.../PALS_env/bin/python3 pypath/preconditioner/tests/test_sparse_learned_schwarz_v1.py。
4. 安装 C 编译器后重新执行 ./autogen.sh、配置和 make -j8。
5. 记录新二进制哈希，再开始阶段二单线路侧车实验。
