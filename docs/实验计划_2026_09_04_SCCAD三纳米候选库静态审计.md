# 实验计划：SCCAD 三纳米候选库静态审计（2026-09-04）

## 目标

审计公开 SCCAD-3nm-PDK 是否包含可由本课题使用的紧凑器件模型和公开数值参考，从而判断它是否值得进入 ngspice 接入实验。该阶段不运行仿真，不把物理设计、版图或 Liberty 表征文件误当作晶体管模型。

## 范围与方法

固定仓库 `sjung366/old-repositoru` 的提交 `0b24880a87edac1034475870512d0ec6894ecc0b`。采用无检出浅克隆，仅取得文件树；搜索 Verilog-A、HSPICE/Spectre 模型卡、SPICE 网表、原始波形与 HSPICE 输出。若找到候选文件，只保留路径、散列和不超过必要范围的小型文本副本；不下载版图、工艺提取表和历史对象。

## 资源、停止条件与指标

使用 1 个中央处理器核心，限时 5 分钟。若文件树仅含版图、LEF、Liberty、工艺提取或商业流程文件，立刻停止并删除临时克隆；若含模型则另写接入实验计划。记录仓库提交、许可证、模型文件数、参考波形文件数、标准单元数量、临时数据大小和判定理由。输出仅为 `results/candidates/sccad3_static_audit_20260904/` 下的摘要。
