# 图文件约定

- `architecture.tex` 是可直接编译的结构图。
- 最终实验脚本应导出 `newton_cdf.pdf`，内容为逐阶段 Newton 迭代次数的经验累积分布。
- 最终实验脚本应导出 `scale_speedup.pdf`，内容为按实例数分桶的端到端加速比。
- 两张数据图必须使用矢量 PDF，字体不小于 7 磅，并与 `RESULT_MACROS.tex` 使用同一份汇总数据。
- 若数据图不存在，论文会显示醒目的占位框，防止带占位图误投稿。

